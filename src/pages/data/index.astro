---
import BaseLayout from "~/layouts/BaseLayout.astro";
import { vizTopics, type VizManifest } from "~/data/visualizations";
import inflationManifest from "~/data/charts-manifest-inflation.json";
import gdpManifest from "~/data/charts-manifest-gdp.json";

const manifests: Record<string, VizManifest> = {
  inflation: inflationManifest as unknown as VizManifest,
  gdp: gdpManifest as unknown as VizManifest,
};

const totalCharts = Object.values(manifests).reduce(
  (n, m) => n + m.charts.length,
  0,
);

function fmtDate(iso: string | null) {
  if (!iso) return null;
  return new Date(iso).toLocaleDateString("en-CA", {
    year: "numeric",
    month: "long",
    day: "numeric",
  });
}
---

<BaseLayout
  title="Charts"
  description="High-quality, auto-updated charts on Canadian inflation, GDP, and macroeconomic trends — generated from Statistics Canada microdata."
>
  <!-- Hero ---------------------------------------------------------------->
  <section class="container-wide pt-16 pb-12">
    <header class="reveal max-w-3xl">
      <p
        class="text-xs font-medium tracking-widest uppercase text-accent-700 dark:text-accent-300 mb-3"
      >
        Charts
      </p>
      <h1
        class="font-serif text-4xl sm:text-5xl font-semibold leading-[1.1] tracking-tight"
      >
        Canadian economy in charts
      </h1>
      <p class="mt-4 text-lg text-muted max-w-2xl leading-relaxed">
        Publication-quality visualizations built from Statistics Canada
        microdata — automatically refreshed whenever new data is released.
      </p>
    </header>

    <div class="reveal mt-8 flex flex-wrap items-center gap-x-6 gap-y-2 text-sm">
      <span class="flex items-center gap-1.5 text-muted">
        <span
          class="w-2 h-2 rounded-full bg-emerald-500"
          style="animation: pulse 2s cubic-bezier(0.4,0,0.6,1) infinite"
        ></span>
        Live data · auto-updated
      </span>
      {
        totalCharts > 0 && (
          <span class="text-muted">
            {totalCharts} charts across {vizTopics.length} topics
          </span>
        )
      }
    </div>
  </section>

  <!-- Topic cards --------------------------------------------------------->
  <section class="container-wide pb-16">
    <div class="grid md:grid-cols-2 gap-6">
      {
        vizTopics.map((topic, i) => {
          const manifest = manifests[topic.slug];
          const chartCount = manifest.charts.length;
          const updated = fmtDate(manifest.updated);
          const preview = manifest.charts.slice(0, 3);

          return (
            <a
              href={`/data/${topic.slug}/`}
              class="reveal group relative flex flex-col rounded-2xl border border-default bg-elevated hover:border-accent-300 dark:hover:border-accent-700 hover:shadow-xl transition-all duration-300 overflow-hidden"
              style={`animation-delay:${i * 80}ms`}
            >
              {/* Preview strip */}
              <div class="relative h-44 bg-[color-mix(in_srgb,var(--bg)_60%,transparent)] border-b border-default overflow-hidden grid-noise">
                {preview.length > 0 ? (
                  <div class="absolute inset-2 flex gap-2">
                    {preview.map((chart, j) => (
                      <div
                        class="flex-1 rounded-lg overflow-hidden shadow-md"
                        style={`transform:rotate(${(j - 1) * 1.8}deg) translateY(${Math.abs(j - 1) * 6}px);transition:transform 0.4s ease ${j * 60}ms`}
                      >
                        <img
                          src={`${topic.chartsDir}${chart.filename}`}
                          alt={chart.title}
                          class="w-full h-full object-cover"
                          loading="lazy"
                        />
                      </div>
                    ))}
                  </div>
                ) : (
                  <div class="absolute inset-0 flex flex-col items-center justify-center gap-3">
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      width="40"
                      height="40"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="1.25"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      class="text-muted opacity-40"
                      aria-hidden="true"
                    >
                      <line x1="18" y1="20" x2="18" y2="10" />
                      <line x1="12" y1="20" x2="12" y2="4" />
                      <line x1="6" y1="20" x2="6" y2="14" />
                    </svg>
                    <p class="text-xs text-muted opacity-60">Syncing charts…</p>
                  </div>
                )}
              </div>

              {/* Card body */}
              <div class="flex flex-col flex-1 p-6">
                <div class="flex items-start justify-between gap-4 mb-3">
                  <h2 class="font-serif text-xl font-semibold group-hover:text-accent-600 dark:group-hover:text-accent-400 transition-colors">
                    {topic.title}
                  </h2>
                  {chartCount > 0 && (
                    <span class="flex-shrink-0 text-xs font-medium px-2.5 py-1 rounded-full bg-accent-50 dark:bg-accent-900/30 text-accent-700 dark:text-accent-300">
                      {chartCount} charts
                    </span>
                  )}
                </div>

                <p class="text-sm text-muted leading-relaxed mb-5 flex-1">
                  {topic.description}
                </p>

                <div class="flex items-center justify-between text-xs text-muted border-t border-default pt-4">
                  <span>
                    {updated ? `Updated ${updated}` : "Awaiting first sync"}
                  </span>
                  <span class="flex items-center gap-1 text-accent-600 dark:text-accent-400 font-medium group-hover:gap-2 transition-all">
                    View charts
                    <svg
                      xmlns="http://www.w3.org/2000/svg"
                      width="13"
                      height="13"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2.5"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      aria-hidden="true"
                    >
                      <path d="M5 12h14M12 5l7 7-7 7" />
                    </svg>
                  </span>
                </div>
              </div>
            </a>
          );
        })
      }
    </div>

    <!-- Methodology note -->
    <div
      class="reveal mt-8 flex gap-4 p-6 rounded-2xl border border-default bg-elevated"
    >
      <div class="flex-shrink-0 mt-0.5">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="1.75"
          stroke-linecap="round"
          stroke-linejoin="round"
          class="text-accent-500"
          aria-hidden="true"
        >
          <circle cx="12" cy="12" r="10"/>
          <line x1="12" y1="8" x2="12" y2="12"/>
          <line x1="12" y1="16" x2="12.01" y2="16"/>
        </svg>
      </div>
      <div>
        <h2 class="font-medium text-sm mb-1">About this data</h2>
        <p class="text-sm text-muted leading-relaxed">
          All charts are generated programmatically from{" "}
          <a
            href="https://www.statcan.gc.ca/"
            target="_blank"
            rel="noopener noreferrer"
            class="text-accent-600 dark:text-accent-400 hover:underline"
          >
            Statistics Canada
          </a>{" "}
          microdata using R and custom ggplot2 styling. Code is open source —
          each topic page links to its source repository. Charts are automatically
          regenerated and deployed whenever Statistics Canada publishes new data.
        </p>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.4; }
  }
</style>
